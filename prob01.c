#include<stdio.h>
int main(){
    int len, breath,area;
    printf("Length of rectangle  \n",len);
    scanf("%d",&len);
    printf("breadth  \n",breath);
    scanf("%d",&breath);
    area=len*breath;
    printf("Area of reactangle is %d\n",area);
    return 0;
}